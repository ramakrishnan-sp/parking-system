# Use the official .env.example – copy to .env and fill values
VITE_API_URL=http://localhost:8000
# No map API key needed — uses OpenStreetMap (free, no account required)
VITE_RAZORPAY_KEY_ID=rzp_test_your_key_id_here
